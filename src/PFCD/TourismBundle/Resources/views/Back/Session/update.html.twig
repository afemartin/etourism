{% extends 'PFCDTourismBundle:Back:layout.html.twig' %}

{% form_theme edit_form 'PFCDTourismBundle:Form:fields.html.twig' %}

{% set sectiontab = 'session' %}{% set datepicker = 1 %}

{% block body %}
<h1>{{ 'back.session.update.title'|trans }}</h1>
<p>{{ 'back.session.update.subtitle'|trans }}</p>

<form class="form-horizontal" action="{{ path('back_session_update', { 'id': session.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <div class="form-actions">
        <button class="btn btn-success" type="submit"><i class="icon-ok icon-white"></i> {{ 'Save changes'|trans }}</button>
        <a class="btn btn-inverse" href="{{ path('back_session_read', { 'id': session.id }) }}"><i class="icon-remove icon-white"></i> {{ 'Cancel changes'|trans }}</a>
    </div>
</form>
{% endblock %}