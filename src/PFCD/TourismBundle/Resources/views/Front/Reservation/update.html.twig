{% extends 'PFCDTourismBundle:Front:layout.html.twig' %}

{% form_theme edit_form 'PFCDTourismBundle:Form:fields.html.twig' %}

{% block body %}
<h1>{{ 'front.reservation.update.title'|trans }}</h1>
<p>{{ 'front.reservation.update.subtitle'|trans }}</p>

<form class="form-horizontal" action="{{ path('front_reservation_update', { 'id': reservation.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <div class="form-actions">
        <button class="btn btn-success" type="submit"><i class="icon-ok icon-white"></i> {{ 'Save changes'|trans }}</button>
        <a class="btn btn-inverse" href="{{ path('front_reservation_read', { 'id': reservation.id }) }}"><i class="icon-remove icon-white"></i> {{ 'Cancel changes'|trans }}</a>
    </div>
</form>
{% endblock %}